import { cronStub } from "@/lib/cron-stub";

// Own-listing MLS sync every 15 min during business hours
export const GET = cronStub("Phase 4", "Own-listing MLS sync every 15 min during business hours");

import { cronStub } from "@/lib/cron-stub";

// 6am email digest: day ahead, top leads, empty content slots
export const GET = cronStub("Phase 3", "6am email digest: day ahead, top leads, empty content slots");

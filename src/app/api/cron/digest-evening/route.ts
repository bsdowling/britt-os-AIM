import { cronStub } from "@/lib/cron-stub";

// 4:30pm email digest: still-open items, due tomorrow, unanswered
export const GET = cronStub("Phase 3", "4:30pm email digest: still-open items, due tomorrow, unanswered");
